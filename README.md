# powerinbox-webservice

### Build:
    ./gradlew clean; ./gradlew processResources; ./gradlew -i build;

### Run:
    java -jar build/libs/powerinbox-webservice.jar
### More Info and Configuration in Summary PDF
